apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    app: nginx
    test: test
spec:
  selector:
    app: nginx
    test: test
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP